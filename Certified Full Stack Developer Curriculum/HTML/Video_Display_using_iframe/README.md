# What I Learned
- In-depth usage of the `iframe` element
- Allow attribute `(allow)`
- `allow` values: `accelerometer`, `autoplay`, `clipboard-write`, `encrypted-media`, `gyroscope`, and more.
- Referrer policy attribute `(referrerpolicy)`
- Allow fullscreen attribute `(allowfullscreen)`