# What I Learned
- Injecting JavaScript to HTML through the `<script>` tag
- Selecting an element with the `querySelector()` method
- Selecting an element with the `getElementById()` method
- Structure of an `object`
- `addEventListener()` method
- DOM `style` property, along with `borderColor`